% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_cs_bip.R
\name{sim_mark_cs_bip}
\alias{sim_mark_cs_bip}
\title{Simulate CS-bip mark (arrivals + edges) for one event}
\usage{
sim_mark_cs_bip(net, t_k, params, formula_rhs, model_cache = NULL, eps = 1e-12)
}
\arguments{
\item{net}{pre-event network::network}

\item{t_k}{event time}

\item{params}{list with beta_edges, lambda_new, and CS_* parameters}

\item{formula_rhs}{ERNM RHS string, e.g. "edges + gwesp(0.5, fixed=TRUE)"}

\item{model_cache}{optional cache list(rhs=..., model=...) (passed to edge_probs_cs)}

\item{eps}{clamp}
}
\value{
list(arrivals=data.frame(id,role), edges=data.frame(i,j), model_cache=...)
}
\description{
Rules (BA-bip style):
- exactly 1 new "event" node
- K_new new "perp" nodes, K_new ~ Poisson(lambda_new)
- deterministic edges: event -- each new perp
- optional edges: event -- each OLD perp with CS probability
}

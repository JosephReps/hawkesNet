```{R}
#| include: false

pkg_dir <- Sys.getenv("QUARTO_PROJECT_DIR", unset = getwd())

if ("package:hawkesNet" %in% search())
  detach("package:hawkesNet", unload = TRUE, character.only = TRUE)

if ("hawkesNet" %in% loadedNamespaces())
  unloadNamespace("hawkesNet")

devtools::load_all(pkg_dir, quiet = TRUE)

library(network)
library(sna)
library(withr)
library(ernm)
library(knitr)
library(kableExtra)
```

```{=html}
<style>
.vcenter-cols .columns {
  display: flex;
  align-items: center;
  gap: 0.75rem;
}
.vcenter-cols .columns .column {
  flex: 1 1 0;
}
</style>
```
